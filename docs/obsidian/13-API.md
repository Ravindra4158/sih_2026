---
title: API
---
# API
Key routes: `POST /documents/upload`, `POST /documents/process`, `POST /verification/documents/verify`, `GET /verification`, `GET /verification/{id}`, `GET /documents/{id}/report`, and `GET /health`.

[[Backend]] [[Frontend]] [[Testing]] [[Security]]
