# Threat model

Initial concerns include forged inputs, replay attacks, unauthorized data access, model error, and operator misuse. This scaffold makes no security claims; formal threat modeling must precede deployment.
